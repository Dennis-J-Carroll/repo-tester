services:
  - type: web
    name: repo-tester-api
    runtime: python
    buildCommand: pip install -e ".[server]"
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GITHUB_TOKEN
        sync: false
    healthCheckPath: /health
